"""Geo module (owner A2): catalogue, corridors, stops, route versions, routing adapter,
route matching and the feature-flag core (spec §6, §10.2, §20.3; ADR-0001, ADR-0008).

Other modules use only :mod:`app.modules.geo.service`.
"""
